import 'dart:typed_data';

Future<void> downloadBytes(
    String filename, Uint8List bytes, String mimeType) async {
  throw UnsupportedError(
      'File download is currently supported in the web application.');
}
